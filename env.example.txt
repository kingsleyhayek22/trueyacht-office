# Same Supabase project as Dashboard/Crew (hcgnwhhfpcqiqnjxhlip) — this app
# reuses it rather than standing up a separate one.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Deliberately no SUPABASE_SERVICE_ROLE_KEY here — Office runs entirely on
# the anon key + an authenticated staff session + RLS (is_trueyacht_staff()),
# same "never use service_role in the web app" rule the Dashboard follows.
